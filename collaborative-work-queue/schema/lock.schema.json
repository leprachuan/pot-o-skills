{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Lock State",
  "description": "JSON lock file schema for collaborative-work-queue",
  "type": "object",
  "required": ["state"],
  "properties": {
    "state": {
      "type": "string",
      "enum": ["idle", "active", "reconciling"],
      "description": "Current lock state"
    },
    "item_id": {
      "type": "string",
      "description": "ID of the locked work item"
    },
    "owner": {
      "type": "string",
      "description": "Agent or process that holds the lock"
    },
    "reason": {
      "type": "string",
      "description": "Human-readable reason for the lock action"
    },
    "acquired_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the lock was acquired"
    },
    "ttl_expires_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the lock TTL expires"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of last update"
    }
  }
}
