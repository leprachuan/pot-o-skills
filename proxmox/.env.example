# Proxmox Cluster Configuration
# Copy this file to .env and fill in your values
# .env is git-ignored and should NEVER be committed

# Cluster nodes (comma-separated IPs or hostnames)
PROXMOX_NODES=192.168.1.44,192.168.1.46,192.168.1.47,192.168.1.41,192.168.1.42

# Node names matching above IPs (comma-separated, same order)
PROXMOX_NODE_NAMES=proxmox4,proxmox5,proxmox7,proxmox10,proxmox11

# Primary node used for cluster-wide API calls
PROXMOX_PRIMARY=192.168.1.44

# SSH access
PROXMOX_SSH_USER=root
PROXMOX_SSH_KEY=/home/flipkey/.ssh/id_ed25519

# Proxmox API Token (Datacenter > Permissions > API Tokens in the UI)
# Format: USER@REALM!TOKENID
PROXMOX_API_TOKEN_ID=root@pam!fosterbot
PROXMOX_API_TOKEN_SECRET=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# API port (default 8006)
PROXMOX_API_PORT=8006

# Community scripts repo (default: community-scripts/ProxmoxVE)
PROXMOX_HELPER_SCRIPTS_REPO=community-scripts/ProxmoxVE
